{
  "name": "cicd-demo-app",
  "version": "1.0.0",
  "description": "A sample Node.js app for testing CI/CD pipelines",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
